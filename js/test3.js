
function drawStarsystem() {
    alert('loading!');
/*    var doc = ajaxGetResponse();
    if (!doc) return;
    var dest = window.document.getElementById('planet');
    if (!dest) return;
    
    var rowCount=dest.rows[].length;
    var starsystem = doc.getElementsByTagName('starsystem')[0];
    
    var planets = starsystem.getElementsByTagName('planet');
    for (i in planets) {
	if (rowCount<i)
	{
	    dest.insertRow(i-1);
	    dest.rows[i-1].insertCell(0);
	    dest.rows[i-1].insertCell(1);
	}
	var modRow=dest.rows[i-1].cells;
	modRow[0].innerHTML=planets[i].ring;
	modRow[1].innerHTML=planets[i].getElementsByTagName('pop').data;
    }*/
}
